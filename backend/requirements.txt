# Runtime dependencies (pinned for reproducible production deployments).
# Upgrade deliberately: bump, run `python -m pytest tests`, then re-pin.
fastapi==0.136.1
uvicorn[standard]==0.46.0
httpx==0.28.1
SQLAlchemy==2.0.49
PyJWT==2.13.0
alembic==1.18.4
