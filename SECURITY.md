# Security Policy

Shadow Agent 是一个面向 LLM Agent 运行时的安全中间层。安全是本项目的立身之本，
因此我们非常重视社区报告的每一个安全问题。

## 上报渠道

如果你发现了安全漏洞，请通过**私有渠道**与我们联系，**不要**在公开 issue 中披露细节：

- 邮箱：`Sec-Wang@proton.me`

请在邮件中尽可能包含以下信息，以帮助我们快速定位和修复：

1. 受影响版本（如 `0.2.0`）或 commit hash；
2. 漏洞类型（如提示词注入绕过、越权、敏感信息泄露等）；
3. 最小复现步骤或 PoC（越具体越好）；
4. 你认为的潜在影响（例如：可绕过工具权限校验、可窃取上游 API Key 等）。

## 处理流程与 SLA

收到报告后，维护者会：

1. **3 个工作日内**确认收到报告并给出初步评估；
2. 评估期间与报告者保持沟通，确认影响范围和严重程度；
3. 优先在**私有分支**中开发修复，避免提前泄露；
4. 修复完成后发布补丁版本，并视情况申请 CVE；
5. 公开一份安全公告（advisory），在报告中致谢（除非你要求匿名）。

## 负责任的披露

我们遵循负责任的披露原则：在修复版本发布前，请勿公开漏洞细节。
请给予维护者合理的修复时间；如果在约定的时间窗口内未得到响应，你可以再自行决定后续披露方式。

## 支持范围

| 版本 | 状态 |
| --- | --- |
| `main` 分支最新提交 | ✅ 受支持 |
| 已发布的正式版本 | 视公告而定 |

仅对**当前维护中的版本**提供安全修复。请升级到受支持版本后再上报问题。

## 本项目自身的安全边界

Shadow Agent 的定位是"给现有 Agent 框架加一层安全控制面"，它本身并不能替代：

- 上游 LLM 提供商自身的模型安全能力；
- 操作系统级隔离、容器逃逸防护、网络策略；
- 密钥托管服务（如 KMS / Vault）与最小权限 IAM。

请勿在生产环境直接暴露未启用 TLS 的网关端口。部署前请阅读
[`PUBLIC_ACCESS.md`](./PUBLIC_ACCESS.md) 中的加固建议。

## 常见误报与边界

以下情况**不属于**本项目需要修复的安全漏洞：

- 依赖用户自行配置的环境变量泄露（如把 `.env` 提交到公开仓库）；
- 攻击者已经获得管理员权限后的任意操作；
- 需要物理访问或已拥有主机权限才能触发的场景。

如果你不确定某个问题是否属于安全漏洞，也可以先通过私有渠道询问。
